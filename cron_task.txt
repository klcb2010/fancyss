# BEGIN CUSTOM CRON TASK

# 删除旧任务，防止重复
cru d frpc_guard 2>/dev/null
cru d rclone_guard 2>/dev/null
cru d clean_logs 2>/dev/null

# 1. 强制拉起定时守护
cru a frpc_guard "*/5 * * * * [ -z \"\$(pidof frpc)\" ] && /bin/sh /jffs/scripts/frpc_start.sh"
cru a rclone_guard "*/5 * * * * [ -z \"\$(pidof rclone)\" ] && /bin/sh /jffs/scripts/rclone_webdav.sh"

# 2. 其他原有日志清理与更新任务
cru a clean_logs "0 */8 * * * /jffs/scripts/clean_cron_logs.sh

# 3. 立即尝试拉起一次 (后台运行)
/bin/sh /jffs/scripts/frpc_start.sh &
/bin/sh /jffs/scripts/rclone_webdav.sh &

# END CUSTOM CRON TASK
